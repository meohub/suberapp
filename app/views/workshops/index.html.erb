<%= datagrid_form_for @grid, :method => :get, :url => workshops_path %>
<%= link_to "New", new_workshop_path, class: "datagrid-submit" %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>
