<%= simple_form_for(@workshop) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.association :contact, label_method: :to_label%>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
